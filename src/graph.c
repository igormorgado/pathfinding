#include "graph.h"

struct graph
{
    size_t n;                   /* Number of vertices */
    size_t m;                   /* Number of edges */
    struct vertex[MAX_VERTEX];  /* Vertex Array */
};
